





step 1 rough protocol sketch for json

step 2 create the rust crate (binary) - add lib later for api and
other reasons

1: widget based frame (higher level) - mirrors 
yoga boxes and is closer to react components

2: cell-based frame (lower level)
'final raster' more verbose ie cells : x0 y0 fg: white bg black




first i have to understand how reactreconcile + yoga export values




react builds a ui tree off of its components and makes them nodes
then yoga/ink associate x/y/width/height to these nodes
	logic wrapper system


we need to lock in the json shape that takes yoga data in


step 1 find out everything extractable from yoga


you give yoga the root size of your window and it performs 
calculations for the layout positions from the react components

so doing a higher level json (instead of a cell sized json)
just takes the boxes translated for the terminal and
sends them to rust


if you drop to raw cell grid youre throwing away yogas work
and redoing a bunch of low level work with drawing the cells



before step 1: 
load in the reconcile and yoga functions that are needed - 
fork it over from ink, because ink is battle tested



so we made it so that theres a rust crate

rust + js process

both independent but under the same repo

js frontend rust backend


and json communicating between the two
