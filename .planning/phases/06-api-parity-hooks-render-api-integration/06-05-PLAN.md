---
phase: 06-api-parity-hooks-render-api-integration
plan: 05
type: execute
wave: 3
depends_on: ["06-02", "06-03", "06-04"]
files_modified:
  - src/index.ts
  - crates/renderer/src/input.rs
autonomous: true
requirements: [RAPI-01, RAPI-06, RAPI-07]

must_haves:
  truths:
    - "Public API exports all hooks, render functions, components, and types from src/index.ts"
    - "Full tui-engine API is importable via a single import"
  artifacts:
    - path: "src/index.ts"
      provides: "Complete public API surface"
      exports: ["render", "renderToString", "measureElement", "patchConsole", "useInput", "useApp", "useStdin", "useFocus", "useFocusManager", "Box", "Text", "Newline", "Spacer", "Transform", "Static"]
  key_links:
    - from: "src/index.ts"
      to: "src/render/index.ts"
      via: "re-export render API"
      pattern: "export.*from.*render"
    - from: "src/index.ts"
      to: "src/hooks/index.ts"
      via: "re-export hooks"
      pattern: "export.*from.*hooks"
    - from: "src/index.ts"
      to: "src/components/index.ts"
      via: "re-export components"
      pattern: "export.*from.*components"
---

<objective>
Wire the complete public API surface into src/index.ts and verify the full type-check passes across the entire codebase.

Purpose: This makes tui-engine consumable as a single package import, completing the Phase 6 API parity goal.
Output: Updated src/index.ts with full public API, clean type-check.
</objective>

<execution_context>
@C:/Users/Mohammed Ibrahim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mohammed Ibrahim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-api-parity-hooks-render-api-integration/06-CONTEXT.md
@.planning/phases/06-api-parity-hooks-render-api-integration/06-02-SUMMARY.md
@.planning/phases/06-api-parity-hooks-render-api-integration/06-03-SUMMARY.md
@.planning/phases/06-api-parity-hooks-render-api-integration/06-04-SUMMARY.md
@src/index.ts
@src/hooks/index.ts
@src/render/index.ts
@src/components/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire public API index and run full verification</name>
  <files>src/index.ts</files>
  <action>
Update `src/index.ts` to export the complete tui-engine public API surface:

```typescript
// Render API
export { render, renderToString, measureElement, patchConsole } from './render/index.js';
export type { RenderOptions, Instance } from './render/types.js';

// Hooks
export { useInput, useApp, useStdin, useFocus, useFocusManager } from './hooks/index.js';
export type { Key } from './hooks/types.js';

// Components
export { Box, Text, Newline, Spacer, Transform } from './components/index.js';
export { Static } from './components/Static.js';

// Types (re-export useful DOM types for measureElement refs)
export type { DOMElement } from './dom/types.js';
```

This matches Ink's public API surface: render + hooks + components + types.

Also verify: if `src/components/index.ts` doesn't already export all components, update it to include Static.

Run full type-check and test suite to verify everything integrates cleanly.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>src/index.ts exports complete public API. Full type-check passes. Full test suite passes. tui-engine is importable as a single package with all Ink-compatible APIs.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — full codebase type-checks clean
- `npx vitest run` — all tests pass (hooks, render, components, existing tests)
- All public APIs importable from src/index.ts
</verification>

<success_criteria>
The complete Ink-compatible public API is exportable from a single import: render(), renderToString(), measureElement(), patchConsole(), all five hooks, all six components (Box, Text, Newline, Spacer, Transform, Static), and relevant types (Key, RenderOptions, Instance, DOMElement). Full type-check and test suite pass.
</success_criteria>

<output>
After completion, create `.planning/phases/06-api-parity-hooks-render-api-integration/06-05-SUMMARY.md`
</output>
